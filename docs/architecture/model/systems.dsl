gpuDriver = softwareSystem "GPU Driver" "Desktop/mobile GPU driver (GL / D3D / Metal / Vulkan)." {
    tags "External"
}

consoleSdk = softwareSystem "Console SDK" "Console toolchains: libdragon (N64), libogc (GC/Wii), PSL1GHT (PS3)." {
    tags "External"
}

osWindowing = softwareSystem "OS / Windowing" "Win32, X11, Wayland, Cocoa, UIKit, ANativeWindow — provides RenderSurface." {
    tags "External"
}

packageRegistry = softwareSystem "Package Registry" "Forgejo package registry storing compiled .xemesh / .xetex assets." {
    tags "External"
}
